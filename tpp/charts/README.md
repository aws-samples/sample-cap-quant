# charts — Helm Charts for In-house Components

- scorer/ — the Scorer service (Deployment + ServiceMonitor + ConfigMap), Milestone 5
- (optional) tpp-dashboard/ — in-house dashboard; only build it if Grafana turns out to be insufficient
